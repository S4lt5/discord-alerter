# discord-alerter

This should use the discord secrets if available, otherwise ask the user for the discord secret

Since this will also set the user's govee lights red when they get a message, also include that if available
